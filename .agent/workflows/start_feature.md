---
description: Create and switch to a new feature branch for Cinema Machina work
---
// turbo-all
1. Ask for a short branch suffix if not already provided.
2. Run `git checkout -b feature/<suffix>`
3. Run `git status`
4. Confirm the new branch name.
